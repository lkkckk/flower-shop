# 预售与功能精简验收记录

日期：2026-09-19。本次仅修改本地工作区，未提交、推送或部署。

## 修复内容

- 预售商品和照片使用稳定标识。上传完成更新原照片对象，避免删除商品或调整照片顺序后串图；保存状态由当前照片计算，移除的上传项不再阻塞保存。
- 修改登记时，在数据库行锁内再次检查幂等结果。同一请求并发重试返回第一次结果，不同修改仍保留版本冲突保护。
- 删除旧预售创建、编辑、制作、加急及状态推进接口与客户端方法，历史记录保留查询和管理员回收站。
- 清理旧预售表单、商品选择器以及已删除经营模块的独立服务代码，保留共用账务服务及历史数据结构。
- 将预售验收入口改为独立 PostgreSQL schema、独立图片目录和独立服务；结束时仅清理本轮创建的资源，不再关闭或清理业务交班数据。

## 已执行验证

- 生产构建通过；Prisma schema 校验通过；图片和数量相关的 8 项单元测试通过。
- `node tests/cleanup-and-noshift-acceptance.mjs --serve`：隔离数据库完成全部迁移；管理员、店员、收银员访问 22 个停用接口均返回 404。
- 在没有开启交班的情况下完成现金结账、充值、订单补款、客户还款和部分退款，核对账户、积分及付款记录。
- 新预售多图、重复手填编号、并发创建及修改重试、同版本不同请求冲突、管理员回收站、历史预售只读，以及登记不影响账务库存的检查通过。
- Chromium 浏览器真实上传：上传期间删除前一个商品、上传期间交换照片顺序、上传失败后重试；保存后核对商品与照片 URL 关联通过。
- 375×812、768×1024、1024×768、1440×900 表单检查通过，保存按钮位于屏幕内。尺寸切换后等待侧栏动画结束再截图。
- 配送单等待图片加载后生成两页 A4 PDF，渲染逐页检查：店名居中，照片等比完整显示，单张照片不跨页截断。
- `git diff --check` 通过。

## 重跑与证据

先构建项目，再执行 `node tests/cleanup-and-noshift-acceptance.mjs`，脚本会自动清理隔离环境。加 `--serve` 可暂留浏览器验收服务；浏览器地址与测试账号写入忽略提交的 `.cache/registration-browser.json`，向进程输入回车即清理退出。

浏览器登录后进入 `/preorders/new`，使用 Playwright CLI 的 `run-code --filename tests/browser-registration.js` 执行交互检查。截图及 PDF 位于忽略提交的 `output/playwright/`。

## 验证边界

这是一轮针对预售简化及功能删除的本地专项验证，并非全系统所有功能验收。未进行 VPS 发布、生产数据库操作、实体打印机测试或本轮 58mm/80mm 小票回归。

本轮 `--serve` 进程的输入管道已关闭，无法通过回车触发退出；备用清理命令被自动审批审核拦截。因此当前隔离测试服务、schema 和临时图片尚未清理：端口 56532，schema `registration_acceptance_1789747237391_b96f2ce2f635417a`，临时目录 `C:\Users\admin\AppData\Local\Temp\flower-registration-test-dj58v3`。不能将本轮描述为已完成环境清理。
