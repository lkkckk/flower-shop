# 收银台改造验收

当前状态：浏览器交互与布局已验证；实体打印待提供硬件型号后联调。

- source visual truth path: C:/Users/admin/AppData/Local/Temp/codex-clipboard-d19d8c21-560b-41cc-8c69-9136cd683646.jpg
- implementation screenshot path: F:/flower-shop/.cache/pos-mobile-final.png
- 其他截图：F:/flower-shop/.cache/pos-ipad.png、F:/flower-shop/.cache/pos-ipad-portrait.png、F:/flower-shop/.cache/pos-receipt-58mm.png
- viewport: 手机 390×844；平板横屏 1180×820；平板竖屏 820×1180。
- source pixels: 1280×2844；工具显示缩放为 922×2048。
- implementation pixels / CSS size: 分别为 390×844、1180×820、820×1180，截图密度 1。
- state: 已登录隔离收银员，混合单位加购，客户选择，成功结账，小票预览及返回收银。

## 对照范围

参考图与实现截图已在同一工具结果中打开比较。参考是商城截图，本次按用户明确批准的方案改造现有浏览器 POS：保留员工导航、订单切换和现有绿色主题，增加固定结账。未复制手机系统栏、小程序外壳和商城底部导航。两者商品内容、视口比例和筛选状态不同，因此不声称像素级还原。

全图对照检查顶部搜索和分类、左侧分类、图文卡片、价格及加购位置；手机商品卡片和小票截图中文字可直接辨认，无需再放大裁剪。实际本地商品图片文件缺失，卡片如实显示缺图状态，不伪造商品照片。本次未验收真实照片的裁剪与加载效果。

## 五项视觉检查

- 字体：使用项目已有中文字体及系统回退；名称、单位、金额层次明确，长名称最多两行。
- 布局：手机为左侧窄分类＋右侧图文列表；平板按宽度展示图片网格；横屏有常驻清单，竖屏固定底部结账。820×1180 DOM 检查无页面横向溢出。
- 色彩：沿用已有绿色品牌色，参考图的浅灰卡片、淡紫规格标签和暖色加购按钮；选中、缺货、禁用状态可区分。
- 图片：已接商品 imageUrl，缺图和加载失败显示明确状态；真实图片资产尚缺，图片内容保真未验证。
- 文案：单位与价格对应，计数使用“项”避免把不同单位混作“件”；收银员不显示无权限的预售开单入口；打印页明确系统打印的硬件要求。

## 对照迭代

1. 手机初次截图中预售入口占据订单标签宽度，收银员还无对应访问权限。修复为仅有权限的员工展示；最终手机截图已显示完整客户标签和新开一单按钮。
2. 移动端图标原先缺少可读名称；补齐排单、备货、盘点、切换收银员及账户菜单的可访问名称。
3. 最终截图中商品列表与固定底栏无重叠，小票在 58mm 内容宽度内完整显示 3扎、1枝及合计 ¥58.59。

## 已执行交互

- 选择“扎”，连续点击两次直接加入 2 扎，无加入确认弹窗。
- 数量面板改为 3 扎，再切换“枝”加入 1 枝；合计 ¥58.59，可加库存为 69 枝。
- 手机顶部客户入口打开购物车及客户选择；确认后执行真实测试收款。
- 订单保存后清单清空、库存刷新，打开同页小票；小票显示 3扎 ¥56.70、1枝 ¥1.89、实收 ¥58.59。
- 小票页返回收银台，上一笔小票入口仍可访问。
- 浏览器已检查错误/警告日志，未发现该轮业务流程错误。
- 独立 PostgreSQL schema API 回归已通过，覆盖单位、金额、库存与收款流水、非法数量、库存不足回滚、价格变化、折扣、满减、储值、挂账及读取小票。

## 验收限制

- 浏览器视口模拟不等于实体 iPad / Safari 验证。
- 没有真实商品照片，尚需补齐图片后验证图像呈现。
- 未连接 58mm 蓝牙小票机，未验证系统打印面板到实际出纸的链路；打印服务需另行按型号确定。

final result: passed

此结果仅指本次浏览器布局及已执行交互验收，不代表照片保真、实体设备或硬件打印验收通过。
